#!/usr/bin/env python
"""OpenElections management commands"""

# TODO: Deprecate this file.  Eventually we should use setuptools to
# automatically create the command wrapper and encourage people to install
# the openelex package with ``pip install --editable`` instead of just
# cloning the repository

from openelex.tasks import cli

if __name__ == "__main__":
    cli()
